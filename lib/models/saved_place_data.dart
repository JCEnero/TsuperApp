import 'package:flutter/material.dart';

class SavedPlaceData {
  const SavedPlaceData(this.label, this.subtitle, this.icon, this.color);
  final String label, subtitle;
  final IconData icon;
  final Color color;
}
